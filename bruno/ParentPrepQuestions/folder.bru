meta {
  name: ParentPrepQuestions
  seq: 3
}

docs {
  `ParentPrepQuestionsController` — the parent's own meeting-prep questions for a child. Parent-private;
  no educator endpoint by design. Text is plain (markup stripped), 1–500 characters.
}

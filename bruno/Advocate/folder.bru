meta {
  name: Advocate
  seq: 2
}

docs {
  `AdvocateController` — the Virtual Advocate: a parent's private chat threads about a child and the
  streaming message endpoint. Threads are visible only to the parent who started them. No educator
  endpoint by design.
}

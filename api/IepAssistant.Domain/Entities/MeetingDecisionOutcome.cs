namespace IepAssistant.Domain.Entities;

/// <summary>Outcome recorded for a <see cref="MeetingDecision"/> (plan 7, decision 3). Stored as a string.</summary>
public enum MeetingDecisionOutcome
{
    Agreed,
    Disagreed,
    Deferred
}

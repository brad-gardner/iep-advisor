namespace IepAssistant.Domain.Entities;

/// <summary>Lifecycle of a <see cref="DraftResponse"/> (plan 6, decision 4). Stored as a string.</summary>
public enum DraftResponseStatus
{
    Open,
    Resolved
}

namespace IepAssistant.Domain.Entities;

/// <summary>Lifecycle of a post-meeting family <see cref="MeetingSummary"/> (plan 6, decision 6). Stored as a string.</summary>
public enum MeetingSummaryStatus
{
    Draft,
    Sent
}
